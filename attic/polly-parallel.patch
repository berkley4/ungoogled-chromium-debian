--- a/build/config/compiler/BUILD.gn
+++ b/build/config/compiler/BUILD.gn
@@ -2425,6 +2425,16 @@
   }
 }
 
+config("polly_parallel") {
+  cflags = [
+    "-mllvm",
+    "-polly-parallel",
+    "-fopenmp=libgomp",
+  ]
+
+  ldflags = [ "-lgomp" ]
+}
+
 _clang_sample_profile = ""
 if (is_clang && is_a_target_toolchain) {
   if (clang_sample_profile_path != "") {
