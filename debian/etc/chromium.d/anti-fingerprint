# Return generic values for WebGLDebugRendererInfo
export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --enable-features=SpoofWebGLInfo"

# Reduce the amount of language info revealed in Accept-Language request header
# See - https://github.com/explainers-by-googlers/reduce-accept-language
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --reduce-accept-language"
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --reduce-accept-language-http"

# Reduce the amount of system info obatainable through headers and javascript
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --reduced-system-info"
