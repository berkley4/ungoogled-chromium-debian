# Disable hardware decoding/encoding
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --disable-features=VaapiVideoDecoder"
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --disable-accelerated-video-decode"
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --disable-accelerated-video-encode"
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --disable-webrtc-hw-decoding"
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --disable-webrtc-hw-encoding"

# Enable VAAPI hardware decoding
export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --enable-features=VaapiVideoDecoder"

# Enable VAAPI hardware encoding
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --enable-features=VaapiVideoEncoder"


# Possibly needed if using vulkan
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --enable-features=VulkanFromANGLE,DefaultANGLEVulkan"

# Deprecated non-vulkan method
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --disable-features=UseChromeOSDirectVideoDecoder"

# Might be needed
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --disable-features=UseSkiaRenderer"

# Might be worth trying if all else fails
#export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --enable-features=VaapiIgnoreDriverChecks"
