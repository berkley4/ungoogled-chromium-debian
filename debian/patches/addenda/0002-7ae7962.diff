From 7ae79624e4fcea9820baba3e83a0e4f636195bd9 Mon Sep 17 00:00:00 2001
From: Darius Mercadier <dmercadier@chromium.org>
Date: Wed, 16 Sep 2026 14:57:57 +0200
Subject: [PATCH] [M154] [turboshaft] Fix load elimination when map changes in a loop

Original change's description:
> [turboshaft] Fix load elimination when map changes in a loop
>
> When an object's map changes inside a loop (e.g. via an in-object property
> addition) or maps are wiped by a call/allocation,
> LateLoadEliminationAnalyzer's merge_maps must detect the discrepancy
> between the forward-edge snapshot and the backedge snapshot and trigger
> a loop revisit.
>
> Previously, merge_maps lacked a loop revisit check. Because alias
> analysis in pass 1 used the optimistic (stale) forward-edge map, stores
> through aliased pointers failed to invalidate loads, leaving memory
> snapshots identical on both edges and preventing merge_memory from
> detecting the need for a revisit.
>
> This CL adds the missing loop_needs_revisit check in merge_maps and
> cleans up the redundant predecessor map snapshot filtering.
>
> TAG=agy
> CONV=da17fd52-5094-4a27-b768-97cf84ff9eff
>
> Fixed: 556908674
> Change-Id: Ie7838faee3e3f99b2bced71a93e79319500a807e
> Reviewed-on: https://chromium-review.googlesource.com/c/v8/v8/+/8352970
> Commit-Queue: Jakob Kummerow <jkummerow@chromium.org>
> Reviewed-by: Jakob Kummerow <jkummerow@chromium.org>
> Auto-Submit: Darius Mercadier <dmercadier@chromium.org>
> Cr-Commit-Position: refs/heads/main@{#109907}

(cherry picked from commit 1aaed11297edb3e3dea3c45e17f70b175de7d626)

Bug: 563677024,556908674
Change-Id: Ie7838faee3e3f99b2bced71a93e79319500a807e
Reviewed-on: https://chromium-review.googlesource.com/c/v8/v8/+/8439937
Commit-Queue: rubber-stamper@appspot.gserviceaccount.com <rubber-stamper@appspot.gserviceaccount.com>
Auto-Submit: chrome-cherry-picker@chops-service-accounts.iam.gserviceaccount.com <chrome-cherry-picker@chops-service-accounts.iam.gserviceaccount.com>
Bot-Commit: rubber-stamper@appspot.gserviceaccount.com <rubber-stamper@appspot.gserviceaccount.com>
Cr-Commit-Position: refs/branch-heads/15.4@{#24}
Cr-Branched-From: df65d5c8f2932045c5af72cd7ecd0c8002fa631e-refs/heads/15.4.80@{#1}
Cr-Branched-From: 80bc5f0e75b24b5af485e0e46b1ffd7f561f323f-refs/heads/main@{#109578}
---

--- a/v8/src/compiler/turboshaft/late-load-elimination-reducer.cc
+++ b/v8/src/compiler/turboshaft/late-load-elimination-reducer.cc
@@ -800,13 +800,7 @@
       // edge of the inner loop should reflect this restriction.
       predecessor_alias_snapshots_.push_back(pred_snapshots->alias_snapshot);
       predecessor_memory_snapshots_.push_back(pred_snapshots->memory_snapshot);
-      if (p->NeighboringPredecessor() != nullptr || !block->IsLoop() ||
-          block->LastPredecessor() != p) {
-        // We only add a MapSnapshot predecessor for non-backedge predecessor.
-        // This is because maps coming from inside of the loop may be wrong
-        // until a specific check has been executed.
-        predecessor_maps_snapshots_.push_back(pred_snapshots->maps_snapshot);
-      }
+      predecessor_maps_snapshots_.push_back(pred_snapshots->maps_snapshot);
     }
   }
 
@@ -839,10 +833,14 @@
       if (is_empty(pred)) {
         // One of the predecessors doesn't have maps for this object, so we have
         // to assume that this object could have any map.
-        return MapMaskAndOr{};
+        minmax = MapMaskAndOr{};
+        break;
       }
       minmax = CombineMinMax(minmax, pred);
     }
+    if (for_loop_revisit && minmax != predecessors[kForwardEdgeOffset]) {
+      loop_needs_revisit = true;
+    }
     return minmax;
   };
   object_maps_.StartNewSnapshot(base::VectorOf(predecessor_maps_snapshots_),
