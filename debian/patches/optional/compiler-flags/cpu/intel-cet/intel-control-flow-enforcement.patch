--- a/build/config/compiler/BUILD.gn
+++ b/build/config/compiler/BUILD.gn
@@ -394,6 +394,10 @@
       cflags += [ "-fwrapv" ]
     }
 
+    if (is_clang && current_cpu == "x64") {
+      cflags += [ "-fcf-protection" ]
+    }
+
     if (use_lld) {
       ldflags += [ "-fuse-ld=lld" ]
       if (lld_path != "") {
