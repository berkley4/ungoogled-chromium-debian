--- a/build/config/compiler/BUILD.gn
+++ b/build/config/compiler/BUILD.gn
@@ -897,6 +897,11 @@
         "-march=$x64_arch",
         "-mtune=generic",
         "-mavx",
+        "-mavx2",
+        "-mfma",
+        #"-mfma3",
+        #"-mfma4",
+        "-ffp-contract=fast",
       ]
       ldflags += [ "-m64" ]
     } else if (current_cpu == "x86") {
